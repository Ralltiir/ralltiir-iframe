{
  "name": "ralltiir-iframe",
  "version": "4.4.3",
  "description": "A mip service implement",
  "main": "./service.js",
  "scripts": {
    "postversion": "git push && git push --tags",
    "lint": "fecs check --level 2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "fecs": "^1.4.1"
  },
  "amdDependencies": {
    "@searchfe/underscore": "^1.0.1",
    "iframe-shell": "^0.0.3"
  }
}
